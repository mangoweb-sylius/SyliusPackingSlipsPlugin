<!DOCTYPE html>
<html>
<body>

<style>
	*{
		box-sizing: border-box;
	}
	body{
		font-family: Arial, Helvetica, sans-serif;
		font-size: 12px;
		margin: 0;
		padding: 0;
	}
	.slips{
	}
	.slips-page{
		display: flex;
		flex-wrap: wrap;
		align-items: flex-start;
		align-content: flex-start;
		height: 100vh;
		padding: 15px 10px;
		page-break-after: always;
	}
	.slips-slip{
		border-right: 1px solid #ccc;
		border-top: 1px solid #ccc;
		flex: 0 1 33.33%;
		padding: 10px;
		padding-right: 2px;
		padding-bottom: 5px;
		height: calc(100% / 8);
		overflow: hidden;
	}
	.slips-slip.is-completed{
		color: green;
	}
	.slips-slip:nth-last-of-type(-n+3){
		border-bottom: 1px solid #ccc;
	}
	.slips-slip:nth-of-type(3n+1){
		border-left: 1px solid #ccc;
	}
	.slips-header{
		text-align: center;
	}
	.slips-products{
		margin-top: 5px;
		font-size: 12px;
	}
</style>

<div class="slips">
	<div class="slips-page">
		{% for shipment in shipments %}
			{% if loop.index % 25 == 0 %}
				</div>
				<div class="slips-page">
			{% endif %}
			{{ include('@MangoSyliusPackingSlipsPlugin/tdUnit.html.twig') }}
		{% endfor %}
	</div>
</div>

</body>
</html>

